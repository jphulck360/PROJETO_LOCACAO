<%= form_for(mark,:html => { :multipart => true }) do |f| %>
  <% if mark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mark.errors.count, "error") %> prohibited this mark from being saved:</h2>

      <ul>
      <% mark.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'Imagem' %>
    <%= f.file_field :photo %>
  </div>

  <div class="field">
    <%= f.label :marca_desc %>
    <%= f.text_field :marca_desc %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
